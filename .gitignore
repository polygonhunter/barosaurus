# build artifacts
node_modules/
main.js
main.js.map

# dev build output inside the test vault
test-vault/.obsidian/plugins/barosaurus/
test-vault/.obsidian/plugins/hot-reload/

# ocr runtime assets — ~8 MB of tesseract worker, wasm core and traineddata,
# fetched with `npm run fetch-ocr-assets` and shipped as a release asset
assets/ocr/

# obsidian workspace state in the test vault (machine-specific)
test-vault/.obsidian/workspace.json
test-vault/.obsidian/workspace-mobile.json

# misc
.DS_Store

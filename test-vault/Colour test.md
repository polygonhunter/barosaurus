# Colour test

This note exists for gates 1 and 2. Nothing here is real content.

Select the line below, open the bar, and apply a text colour. Then look at it in
Live Preview **and** in Reading view — the two can disagree.

This line has **bold** and *italic* in it.

This line has no markup at all, as a control.

A second paragraph, so the alignment action has a block boundary to work
against. HTML blocks may not contain blank lines, so alignment applies per
block and not across paragraphs.
